[package]
name = "simulator"
edition = "2021"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[lib]
crate-type = ["rlib"]

[profile.release]
lto = "fat"

[dev-dependencies]
more-asserts = "0.3.1"
rand = "0.8.5"
