#[derive(Debug, Clone, Copy, Eq, PartialEq)]
pub enum Condition {
    Normal,
    Good,
    Excellent,
    Poor,
    Pliant,
    Centered,
    Sturdy,
    Malleable,
    Primed,
    GoodOmen,
}
